<!--index.wxml-->
<!--1px = 750/320 = 2.34rpx;-->
<view class="container">

  <view class="remind-box" wx:if="{{remind}}">
    <image class="remind-img" src="/images/more/loading.gif"></image>
  </view>

  <block wx:else >
    <view style="font-size: 50rpx;margin-top: 8%;">
      AYL 运动蓝牙耳机
    </view>
    <view class="swiper_box">
      <view wx:for="{{goods}}" wx:key="id">
        <view class="swiper_item">
          <view>
            <image mode="aspectFill" data-id="{{item.id}}" bindtap="toDetailsTap" src="{{item.pic}}_m" lazy-load="true"/>
          </view>
          <view class="title">{{item.characteristic}}</view>
          <view class="flex-wrp">
            <view class="goods-minPrice">￥{{item.minPrice}}</view>
            <view status='del' del-color="#aaa" class="goods-originalPrice">￥{{item.originalPrice}}</view>
          </view>
        </view>
      </view>
    </view>
  </block>
  
</view>
